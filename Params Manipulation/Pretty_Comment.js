/* Credits: 
Added this pretty comment from Clark Winters solution as an example to adhere and to practice across all projects */
___________________________________________________________________________________

/**
 * Creates a new element, appends it to the parent, and adds attributes
 * @param {string} element - The type of element to be created
 * @param {string} parent - Passed to a query selector to select the parent element
 * @param {object} attributes - Key/value pairs representing attributes and their values
 * @returns {element} - The newly created DOM element
 */
function append(element, parent, attributes) {
    
    }
    
}